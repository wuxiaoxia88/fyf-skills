# gemini-picture

OpenClaw Skill：通过 Gemini 网页版批量生成图片，并将生成结果保存到本地目录。

当前版本：`0.1.0`，作为后续升级的基线版本。

## Quick Start

```bash
node generate.mjs <prompts.json> [输出目录]
```

`prompts.json` 示例：

```json
[
  "请生成一幅中国古典山水风格画作，16:9"
]
```

更多说明见 `SKILL.md`。
